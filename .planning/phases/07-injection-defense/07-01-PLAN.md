---
phase: 07
plan: 01
title: Sanitizer package
status: complete
completed: 2026-05-02
commit: 6e2793f
---

# Plan 07-01: internal/sanitizer package

## Objective
Implement `internal/sanitizer` — strip invisible Unicode and NFKC-normalize text before it reaches the summarization pipeline.

## Requirements
SEC-01, SEC-02, SEC-03

## Tasks

- [x] Create `internal/sanitizer/sanitizer.go`
- [x] Implement `StripInvisible(text string) string` — removes Unicode Cf category, bidi controls (U+202A–U+202E), zero-width chars (U+200B–U+200F), PUA block, Tags block (U+E0000–U+E01FF)
- [x] Implement `NormalizeUnicode(text string) string` — NFKC normalization via `golang.org/x/text/unicode/norm`
- [x] Implement `SanitizeAll(text string) string` — StripInvisible then NormalizeUnicode
- [x] Implement `ReportInvisibles(text string) []InvisibleRune` — returns offset, rune, name, category for each stripped character
- [x] Write 31 unit tests covering all categories of invisible chars, edge cases (empty, ASCII, normal Unicode), and normalization output
- [x] Upgrade `golang.org/x/text` from v0.22 to v0.36 in go.mod/go.sum

## Files Modified
- `internal/sanitizer/sanitizer.go` (new)
- `internal/sanitizer/sanitizer_test.go` (new)
- `go.mod` (x/text version bump)
- `go.sum` (updated)

## Verification
- `go test ./internal/sanitizer/...` — 31 tests pass
- `go build ./...` — clean build
