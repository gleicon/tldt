---
phase: 07
plan: 03
title: CLI wiring and matrix integration
status: complete
completed: 2026-05-02
commit: 498cd95
---

# Plan 07-03: CLI wiring, MatrixSummarizer interface, README

## Objective
Wire sanitizer and detector into main.go; expose LexRank similarity matrix via MatrixSummarizer interface so DetectOutliers gets real cosine similarity values; update README with injection defense section.

## Requirements
SEC-01 through SEC-09

## Tasks

- [x] Add `MatrixSummarizer` interface to `internal/summarizer/lexrank.go`
  - `SummarizeWithMatrix(text string, n int) ([]string, [][]float64, error)`
  - Returns pre-normalization cosine similarity matrix alongside top-N sentences
- [x] Refactor `LexRank.Summarize` to delegate to `SummarizeWithMatrix` (no logic duplication)
- [x] Snapshot raw cosine matrix before `rowNormalize` — outlier detection needs values in [0,1], not stochastic rows
- [x] Wire `--sanitize` flag into main.go pre-summarization step
- [x] Wire `--detect-injection` flag: pattern+encoding detection runs before summarization; outlier detection runs after (needs matrix)
- [x] In main.go summarization dispatch: check `MatrixSummarizer` interface; use `SummarizeWithMatrix` when available; fall back to `Summarize` for other algorithms
- [x] After summarization, if `--detect-injection` and `simMatrix != nil`: call `DetectOutliers` with tokenized sentences and matrix
- [x] Remove `_ = *injectionThreshold` placeholder
- [x] Update README: add URL input, config file, Claude Code skill, and injection defense sections; update flags table

## Files Modified
- `internal/summarizer/lexrank.go` — MatrixSummarizer interface + SummarizeWithMatrix method
- `cmd/tldt/main.go` — sanitize/detect-injection wiring, MatrixSummarizer dispatch, outlier post-pass
- `README.md` — URL input, config, skill integration, injection defense sections

## Verification
- `go build ./...` — clean build
- `go test ./...` — 292 tests pass (all packages)
- Smoke test: injection sentence scores 1.00 outlier; pattern detection fires with 0.95 confidence
