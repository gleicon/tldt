---
phase: 09-pii-detection
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - DOC-01

must_haves:
  truths:
    - "README contains a ## Security section"
    - "Section covers LLM04 architectural immunity (no ML weights)"
    - "Section covers LLM08 architectural immunity (no vector store)"
    - "Section covers LLM09 architectural immunity (extractive = no hallucination)"
    - "Section links to docs/security.md for full OWASP coverage"
  artifacts:
    - path: "README.md"
      provides: "## Security section with three named paragraphs and link"
      contains: "## Security"
  key_links:
    - from: "README.md ## Security"
      to: "docs/security.md"
      via: "markdown link"
      pattern: "docs/security.md"
---

<objective>
Add a standalone `## Security` section to README.md covering architectural immunity to LLM04, LLM08, and LLM09 from OWASP LLM Top 10 2025. Three paragraphs, one per category, each 2-3 sentences. Links to docs/security.md for full coverage (LLM01/02/05/10 live there).

Purpose: Security-conscious evaluators scanning the README get the architectural rationale without visiting a separate document — satisfies DOC-01.
Output: Updated README.md with ## Security section placed after the flags table, before ## Installation.
</objective>

<execution_context>
@/Users/gleicon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gleicon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-pii-detection/09-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ## Security section to README.md</name>
  <files>README.md</files>
  <action>
Read README.md in full first. Identify the line after the `## Flags` section (currently around line 82-112). Insert the new `## Security` section after `## Prompt injection defense` (currently the last section before `## Build & test`) — per D-09, placement is after the flags table, before installation, but in practice "before ## Build & test" is the natural security posture location in this README.

Check the actual section order: look for `## Build & test` or `## Installation` headings and insert the `## Security` block immediately before `## Build & test`.

Content to insert:

```markdown
## Security

tldt's architecture provides structural immunity to three OWASP LLM Top 10 2025 categories:

**LLM04 — Model Denial of Service**: tldt is a pure CLI binary. There is no model server, no inference endpoint, and no shared resource that a caller can exhaust. Each invocation is an isolated process that exits when summarization completes — no pooling, no queuing, no per-request GPU allocation.

**LLM08 — Vector and Embedding Weaknesses**: tldt uses no embeddings and no vector store. Similarity scores are computed from raw TF-IDF cosine similarity and word-overlap ratios on the input text alone. There is no persistent index to poison, no retrieval path to manipulate, and no external knowledge base to corrupt.

**LLM09 — Misinformation**: tldt is a purely extractive summarizer. Every sentence in the output is copied verbatim from the source document — no paraphrasing, no generation, no inference. Hallucination is structurally impossible: if a sentence appears in the summary, it existed in the input.

For full OWASP LLM Top 10 2025 coverage including LLM01 (prompt injection defense), LLM02 (insecure output handling), LLM05 (supply chain), and LLM10 (model theft), see [docs/security.md](docs/security.md).
```

Insert this block immediately before the `## Build & test` heading line. Do not modify any other section.
  </action>
  <verify>
    <automated>cd /Users/gleicon/code/go/src/github.com/gleicon/tldt && grep -c '## Security' README.md && grep -c 'LLM04' README.md && grep -c 'LLM08' README.md && grep -c 'LLM09' README.md && grep -c 'docs/security.md' README.md</automated>
  </verify>
  <done>
    - README.md contains `## Security` heading
    - README.md contains paragraphs for LLM04, LLM08, and LLM09
    - README.md contains a link to docs/security.md
    - No other README sections are modified
    - `go build ./...` still succeeds (README is not a Go file, but verifies no accidental edits to .go files)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README content → reader | Documentation only; no code execution boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-02-01 | Tampering | README.md | accept | Documentation file; no security enforcement via README content; rationale accuracy is editorial quality, not a security control |
</threat_model>

<verification>
```bash
cd /Users/gleicon/code/go/src/github.com/gleicon/tldt
grep -A 20 '## Security' README.md
```

The output must show LLM04, LLM08, and LLM09 paragraphs and the docs/security.md link.
</verification>

<success_criteria>
- `grep -c '## Security' README.md` returns 1
- `grep -c 'LLM04\|LLM08\|LLM09' README.md` returns 3 (one per line)
- `grep 'docs/security.md' README.md` returns at least one match
- README section order unchanged for all other sections
</success_criteria>

<output>
After completion, create `.planning/phases/09-pii-detection/09-02-SUMMARY.md`
</output>
