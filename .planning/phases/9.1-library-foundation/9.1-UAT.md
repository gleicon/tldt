---
status: testing
phase: 9.1-library-foundation
source:
  - 9.1-01-SUMMARY.md
  - 9.1-02-SUMMARY.md
started: 2026-05-06T17:55:00Z
updated: 2026-05-06T17:55:00Z
---

## Current Test

number: 1
name: Library API - Cold Start Test
expected: |
  Run `go test ./pkg/tldt/... -v` and see all 25 tests pass (16 original + 9 new edge case tests).
  Tests should cover: Summarize all algorithms, Detect with clean/injection text, Sanitize with clean/invisible chars,
  Pipeline full flow, FetchResult type, Sentinel errors (ErrSSRFBlocked, ErrRedirectLimit, ErrHTTPError, ErrNonHTML).
awaiting: user response

## Tests

### 1. Library API - Cold Start Test
expected: |
  Run `go test ./pkg/tldt/... -v` and see all 25 tests pass (16 original + 9 new edge case tests).
  Tests should cover: Summarize all algorithms, Detect with clean/injection text, Sanitize with clean/invisible chars,
  Pipeline full flow, FetchResult type, Sentinel errors (ErrSSRFBlocked, ErrRedirectLimit, ErrHTTPError, ErrNonHTML).
result: pending

### 2. Library API - FetchResult Type Verification
expected: |
  pkg/tldt.Fetch() returns FetchResult struct with fields: Text (string), StatusCode (int), ContentType (string), FinalURL (string).
  Callers can access result.Text to get extracted article text.
result: pending

### 3. Library API - Sentinel Error Checking
expected: |
  Errors from pkg/tldt.Fetch() can be checked with errors.Is() for: ErrSSRFBlocked, ErrRedirectLimit, ErrHTTPError, ErrNonHTML.
  Error messages contain "tldt.Fetch:" prefix for context.
result: pending

### 4. Library API - Zero Import Internal Check
expected: |
  pkg/tldt/tldt_test.go imports ONLY "github.com/gleicon/tldt/pkg/tldt" — zero imports from internal/ packages.
  This proves the public API contract is complete without requiring internal knowledge.
result: pending

### 5. CLI Refactor - Binary Builds Successfully
expected: |
  Run `go build ./cmd/tldt` and binary compiles without errors.
  The cmd/tldt/main.go now uses `tldt.Fetch()` and `tldt.Detect()` instead of `fetcher.Fetch()` and `detector.Analyze()`.
result: pending

### 6. CLI Refactor - Full Test Suite Passes
expected: |
  Run `go test ./...` and all 353 tests pass across 9 packages with no regressions.
result: pending

### 7. CLI Refactor - Smoke Test Basic Functionality
expected: |
  Run `echo "Hello world. This is a test. Testing is important." | ./tldt` and see 3-sentence summary output:
  "Hello world.
  This is a test.
  Testing is important."
result: pending

### 8. CLI Refactor - URL Fetch via tldt.Fetch
expected: |
  Run `./tldt --url http://example.com` (or any valid URL) and the binary fetches, extracts text, and produces summary.
  --url flag continues to work after refactor to tldt.Fetch.
result: pending

### 9. CLI Refactor - Injection Detection via tldt.Detect
expected: |
  Run `./tldt --detect-injection --url <url>` or with piped input containing injection text.
  Warning messages should appear on stderr for suspicious patterns.
result: pending

### 10. CLI Refactor - Kept Internal Imports Still Work
expected: |
  The following features still work (using kept internal imports):
  - --sanitize flag (sanitizer.SanitizeAll)
  - --detect-pii and --sanitize-pii (detector.DetectPII, detector.SanitizePII)
  - Summarization (summarizer.New and algorithms)
  - --explain flag (summarizer.Explainer interface)
result: pending

## Summary

total: 10
passed: 0
issues: 0
pending: 10
skipped: 0

## Gaps

[none yet]
