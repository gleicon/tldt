---
phase: 9.1-library-foundation
plan: 02
type: execute
wave: 2
depends_on: [9.1-01]
files_modified: [cmd/tldt/main.go]
autonomous: true
requirements: [LIB-CORE-03]
must_haves:
  truths:
    - "cmd/tldt/main.go imports zero internal/summarizer, internal/detector, internal/sanitizer, internal/fetcher packages"
    - "All summarize, detect, sanitize, fetch operations route through pkg/tldt public functions"
    - "344+ tests pass with no regressions after the atomic refactor"
  artifacts:
    - path: "cmd/tldt/main.go"
      provides: "CLI entry point with pkg/tldt imports only for core operations"
  key_links:
    - from: "cmd/tldt/main.go main()"
      to: "pkg/tldt.Summarize()"
      via: "call with tldt.SummarizeOptions"
    - from: "cmd/tldt/main.go resolveInputBytes()"
      to: "pkg/tldt.Fetch()"
      via: "call tldt.Fetch with FetchOptions"
    - from: "cmd/tldt/main.go"
      to: "internal/config, internal/formatter, internal/installer"
      via: "kept as direct imports per D-05 scope clarification"
---

<objective>
Perform atomic refactor of cmd/tldt/main.go to route all core text-processing operations (summarize, detect, sanitize, fetch) through the pkg/tldt public API surface. Remove direct imports of internal/summarizer, internal/detector, internal/sanitizer, and internal/fetcher. Keep internal/config, internal/formatter, internal/installer as direct imports per D-05 scope clarification. All 344+ existing tests must pass with no behavioral regressions.

Purpose: Prove pkg/tldt is a complete, externally-importable library surface and enable Phase 10 to extend it with PII APIs without touching main.go again.

Output: cmd/tldt/main.go with pkg/tldt import added, 4 internal imports removed, all function calls updated to use tldt.Summarize, tldt.Detect, tldt.Sanitize, tldt.Fetch, tldt.Pipeline where appropriate.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/9.1-library-foundation/9.1-CONTEXT.md
@.planning/phases/9.1-library-foundation/9.1-01-SUMMARY.md
@.planning/REQUIREMENTS.md § LIB-CORE-03

## Current main.go Structure

From analysis of cmd/tldt/main.go:
- Line count: 417 lines
- Imports to REMOVE: internal/summarizer, internal/detector, internal/sanitizer, internal/fetcher
- Imports to KEEP: internal/config, internal/formatter, internal/installer (per D-05)
- Import to ADD: tldt "github.com/gleicon/tldt/pkg/tldt"

## Mapping internal to pkg/tldt

| Current (internal) | New (pkg/tldt) |
|------------------|----------------|
| summarizer.New(alg) + s.Summarize(text,n) | tldt.Summarize(text, tldt.SummarizeOptions{Algorithm: alg, Sentences: n}) |
| detector.Analyze(text) | tldt.Detect(text, tldt.DetectOptions{OutlierThreshold: threshold}) |
| detector.DefaultOutlierThreshold | hardcode 0.85 or add to pkg/tldt |
| sanitizer.SanitizeAll(text) | tldt.Sanitize(text) - returns (cleaned, report, error) |
| fetcher.Fetch(url, timeout, maxBytes) | tldt.Fetch(url, tldt.FetchOptions{Timeout: timeout, MaxBytes: maxBytes}) |

## Context Decisions

**D-04:** Replace all 4 internal imports in ONE atomic commit. 417 lines updated. 344 existing tests as safety net.

**D-05:** internal/config, internal/formatter, internal/installer remain direct imports - CLI-specific infrastructure.

**D-03:** pkg/tldt functions wrap errors with context. main.go error handling remains same (check err != nil).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Analyze main.go internal package usage patterns</name>
  <files>cmd/tldt/main.go</files>
  <action>
Before refactoring, document all call sites for the 4 internal packages using grep to find exact usage:

- grep -n "summarizer." cmd/tldt/main.go - Find all summarizer usages
- grep -n "detector." cmd/tldt/main.go - Find all detector usages  
- grep -n "sanitizer." cmd/tldt/main.go - Find all sanitizer usages
- grep -n "fetcher." cmd/tldt/main.go - Find all fetcher usages
- grep -n "MatrixSummarizer" cmd/tldt/main.go - Check for type assertions
- grep -n "DefaultOutlierThreshold" cmd/tldt/main.go - Check flag defaults

Document each call site with: line number, current call pattern, replacement pkg/tldt call, any complexity (return type changes, option struct needed).

Pay special attention to:
1. Whether MatrixSummarizer is actually used in main.go
2. Whether detector.DefaultOutlierThreshold is used (likely for flag default)
3. How detector.Finding or detector.Report types are used
4. Whether sanitizer.InvisibleReport is used directly
5. How fetcher errors are checked (ErrSSRFBlocked, ErrRedirectLimit)
  </action>
  <verify>
    <automated>cd /Users/gleicon/code/go/src/github.com/gleicon/tldt && grep -c "summarizer." cmd/tldt/main.go && grep -c "detector." cmd/tldt/main.go && grep -c "sanitizer." cmd/tldt/main.go && grep -c "fetcher." cmd/tldt/main.go</automated>
  </verify>
  <done>Grep analysis complete - exact count of internal package call sites documented, complexity points identified</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Atomic refactor - replace 4 internal imports with pkg/tldt</name>
  <files>cmd/tldt/main.go</files>
  <behavior>
    - Import block adds: tldt "github.com/gleicon/tldt/pkg/tldt"
    - Import block removes: internal/summarizer, internal/detector, internal/sanitizer, internal/fetcher
    - Summarize calls: tldt.Summarize(text, tldt.SummarizeOptions{Algorithm: alg, Sentences: n})
    - Fetch calls: result, err := tldt.Fetch(url, opts); text = result.Text
    - Sanitize calls: cleaned, report, err := tldt.Sanitize(text)
    - Detect calls: dresult, err := tldt.Detect(text, tldt.DetectOptions{OutlierThreshold: threshold})
    - DefaultOutlierThreshold: use hardcoded 0.85 or add to pkg/tldt if needed
    - All error checking remains unchanged (err != nil)
    - config/formatter/installer imports remain unchanged per D-05
  </behavior>
  <action>
Per D-04, perform atomic refactor of cmd/tldt/main.go. This is a single commit changing 417 lines.

Step 1: Update imports (lines ~1-25)
Add to imports: tldt "github.com/gleicon/tldt/pkg/tldt"
Remove from imports: github.com/gleicon/tldt/internal/detector, internal/fetcher, internal/sanitizer, internal/summarizer
KEEP these imports: github.com/gleicon/tldt/internal/config, internal/formatter, internal/installer

Step 2: Update flag defaults
Find: injectionThreshold := flag.Float64("injection-threshold", detector.DefaultOutlierThreshold, ...)
Change to: injectionThreshold := flag.Float64("injection-threshold", 0.85, ...) or use tldt.DefaultOutlierThreshold if exported

Step 3: Update summarize call sites
Find all summarizer.New(alg) and replace with direct tldt.Summarize() calls.

Old pattern:
s, err := summarizer.New(algorithm)
if err != nil { ... }
sentences, err := s.Summarize(text, numSentences)

New pattern:
result, err := tldt.Summarize(text, tldt.SummarizeOptions{
    Algorithm: algorithm,
    Sentences: numSentences,
})
if err != nil { ... }
summary := result.Summary

Step 4: Update Fetch call sites (in resolveInputBytes)
Old: text, err := fetcher.Fetch(urlStr, timeout, maxBytes)
New: fresult, err := tldt.Fetch(urlStr, tldt.FetchOptions{Timeout: timeout, MaxBytes: maxBytes})
if err != nil { ... }
text := fresult.Text

Step 5: Update sanitizer calls
Old:
cleaned := sanitizer.SanitizeAll(text)
inv := sanitizer.ReportInvisibles(text)

New:
cleaned, sreport, err := tldt.Sanitize(text)
if err != nil { ... }
removedCount := sreport.RemovedCount

Step 6: Update detector calls
Old:
report := detector.Analyze(text)
if report.Suspicious { ... }

New:
dresult, err := tldt.Detect(text, tldt.DetectOptions{
    OutlierThreshold: *injectionThreshold,
})
if err != nil { ... }
if dresult.Report.Suspicious { ... }

Step 7: Handle type-specific usage
If main.go uses detector.Finding or detector.Report types directly:
- detector.Report -> tldt.DetectResult.Report (if exported)
- If tldt.DetectResult does not export Report field, may need to expose it

If main.go uses summarizer.MatrixSummarizer:
- Check if this is actually used in main.go or only passed to detector
- If main.go does not call SimilarityMatrix directly, tldt.Pipeline may handle internally
- If main.go needs it, pkg/tldt may need new exported function

Step 8: Compile and fix
Run: go build ./cmd/tldt
Fix any compile errors - mostly type mismatches from the changes above.

CRITICAL: Do NOT change behavior - only routing. All error messages, exit codes, output formats must remain identical.
  </action>
  <verify>
    <automated>cd /Users/gleicon/code/go/src/github.com/gleicon/tldt && go build ./cmd/tldt && echo "Build successful"</automated>
  </verify>
  <done>main.go compiles successfully with pkg/tldt imports, zero internal/summarizer/detector/sanitizer/fetcher imports</done>
</task>

<task type="auto">
  <name>Task 3: Run full test suite and verify zero regressions</name>
  <files>cmd/tldt/main.go, all test files</files>
  <action>
Per LIB-CORE-03 requirement, verify all 344+ tests pass with no regressions:

cd /Users/gleicon/code/go/src/github.com/gleicon/tldt

Run all tests:
go test ./... 2>&1 | tee test-output.log

Count total tests:
grep -E "^(PASS|FAIL|ok|FAIL)" test-output.log

Verify internal import grep returns zero matches:
grep -E "github.com/gleicon/tldt/internal/(summarizer|detector|sanitizer|fetcher)" cmd/tldt/main.go
Should return NO lines

Expected result:
- go test ./... passes with no FAIL lines
- Total test count should be 344+ (matching pre-refactor count)
- grep for internal imports returns empty

If any tests fail:
1. Identify if it is a behavioral change (unexpected) or test issue
2. Behavioral changes must be reverted - refactor should be transparent
3. Test failures due to import path changes in test files are acceptable to fix

Also verify the binary builds and basic smoke test works:
go build -o /tmp/tldt ./cmd/tldt
echo "Hello world. This is a test. Testing is important." | /tmp/tldt
  </action>
  <verify>
    <automated>cd /Users/gleicon/code/go/src/github.com/gleicon/tldt && go test ./... 2>&1 | grep -E "^(PASS|FAIL)" | tail -5</automated>
  </verify>
  <done>All 344+ tests pass, grep for internal imports returns zero matches, binary builds and runs</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cmd/tldt -> pkg/tldt | Public API boundary - all core operations route through stable library surface |
| pkg/tldt -> internal/* | Internal implementation detail - shielded from CLI consumers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9.1-02-01 | Tampering | Import path change | accept | No security boundary change - same code, different import path |
| T-9.1-02-02 | Repudiation | Test regression | mitigate | Full test suite (344+ tests) validates identical behavior |
</threat_model>

<verification>
Final verification checklist:
- [ ] grep -E "github.com/gleicon/tldt/internal/(summarizer|detector|sanitizer|fetcher)" cmd/tldt/main.go returns NO matches
- [ ] grep "github.com/gleicon/tldt/pkg/tldt" cmd/tldt/main.go shows the import exists
- [ ] go test ./... passes all 344+ tests
- [ ] go build ./cmd/tldt produces working binary
- [ ] Echo test: echo "Test sentence one. Test sentence two." | ./tldt produces non-empty summary
- [ ] All existing CLI flags work: --algorithm, --sentences, --sanitize, --detect-injection, --url
</verification>

<success_criteria>
1. cmd/tldt/main.go imports "github.com/gleicon/tldt/pkg/tldt"
2. cmd/tldt/main.go has ZERO imports of internal/summarizer, internal/detector, internal/sanitizer, internal/fetcher
3. All summarize calls route through tldt.Summarize()
4. All fetch calls route through tldt.Fetch()
5. All sanitize calls route through tldt.Sanitize()
6. All detect calls route through tldt.Detect()
7. go test ./... passes with no failures (344+ tests)
8. Binary builds and produces identical CLI behavior
</success_criteria>

<output>
After completion, create .planning/phases/9.1-library-foundation/9.1-02-SUMMARY.md
</output>
