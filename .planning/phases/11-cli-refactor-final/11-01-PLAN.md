---
phase: 11-cli-refactor-final
plan: 01
type: execute
wave: 1
depends_on: [10-02]
files_modified:
  - pkg/tldt/tldt.go
  - cmd/tldt/main.go
autonomous: true
requirements:
  - CLI-10
  - CLI-11
must_haves:
  truths:
    - "cmd/tldt/main.go imports zero internal/detector, internal/sanitizer, internal/summarizer"
    - "All existing CLI flags (--detect-pii, --sanitize-pii, --sanitize, --detect-injection, etc.) work identically"
    - "360+ tests pass with no regressions"
  artifacts:
    - path: "pkg/tldt/tldt.go"
      provides: "Extended public API with SanitizeAll, DetectOutliers, TokenizeSentences, EvalROUGE, NewSummarizer, Explainer, MatrixSummarizer types"
      min_lines: 400
    - path: "cmd/tldt/main.go"
      provides: "CLI entry point with only internal/config, internal/formatter, internal/installer imports"
      max_internal_imports: 3
  key_links:
    - from: "main.go"
      to: "pkg/tldt.DefaultOutlierThreshold"
      via: "tldt.DefaultOutlierThreshold constant"
      pattern: "tldt\.DefaultOutlierThreshold"
    - from: "main.go"
      to: "pkg/tldt.SanitizeAll"
      via: "tldt.SanitizeAll(text)"
      pattern: "tldt\.SanitizeAll"
    - from: "main.go"
      to: "pkg/tldt.NewSummarizer"
      via: "tldt.NewSummarizer(algo)"
      pattern: "tldt\.NewSummarizer"
---

# Phase 11: CLI Refactor — Zero Internal Imports

## Objective

Complete the zero-internal-imports goal by removing all remaining `internal/detector`, `internal/sanitizer`, and `internal/summarizer` imports from `cmd/tldt/main.go`. The CLI will route all logic exclusively through `pkg/tldt` public API, leaving only `internal/config`, `internal/formatter`, and `internal/installer` as legitimate internal imports per D-05.

**Purpose:** Achieve LIB-CORE-03 compliance — `cmd/tldt/main.go` does not directly import any `internal/` packages except CLI-specific infrastructure.

**Output:** 
- Extended `pkg/tldt` public API with sanitizer and summarizer wrappers
- Refactored `cmd/tldt/main.go` with zero detector/sanitizer/summarizer imports
- All 360+ tests passing with no behavioral regressions

## Execution Context

@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md

## Context

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@pkg/tldt/tldt.go
@cmd/tldt/main.go

### Current Imports in main.go (MUST REMOVE)

1. **internal/detector** (line 17):
   - `detector.DefaultOutlierThreshold` (constant at line 42)
   - `detector.SanitizePII(text)` → NOW in pkg/tldt via Phase 10
   - `detector.DetectPII(text)` → NOW in pkg/tldt via Phase 10  
   - `detector.DetectOutliers(sentences, simMatrix, threshold)` (line 254) → NEEDS pkg/tldt wrapper

2. **internal/sanitizer** (line 20):
   - `sanitizer.SanitizeAll(text)` (line 136) → NEEDS pkg/tldt wrapper
   - `sanitizer.ReportInvisibles(text)` (lines 138, 172) → NEEDS pkg/tldt wrapper

3. **internal/summarizer** (line 21):
   - `summarizer.New(effectiveAlgorithm)` (line 204) → NEEDS pkg/tldt wrapper
   - `summarizer.Explainer` interface (line 214) → NEEDS pkg/tldt type alias
   - `summarizer.ExplainInfo` (line 215) → NEEDS pkg/tldt type alias
   - `summarizer.EvalROUGE(result, refSents)` (line 271) → NEEDS pkg/tldt wrapper
   - `summarizer.MatrixSummarizer` interface (line 235) → NEEDS pkg/tldt type alias
   - `summarizer.TokenizeSentences(text)` (lines 253, 293, 270) → NEEDS pkg/tldt wrapper

### Imports That REMAIN (per D-05 — CLI infrastructure)

- `internal/config` — configuration management
- `internal/formatter` — output formatting  
- `internal/installer` — skill installation

### Key Interfaces to Re-export from pkg/tldt

From `internal/summarizer` (explain.go):
```go
type Explainer interface {
    SummarizeExplain(text string, n int) ([]string, *ExplainInfo, error)
}

type ExplainInfo struct {
    Algorithm   string
    SentencesIn int
    Sentences   []SentenceScore
    Converged   bool
    Iterations  int
    Epsilon     float64
}

type SentenceScore struct {
    Index int
    Score float64
    Text  string
    Selected bool
}
```

From `internal/summarizer` (lexrank.go):
```go
type MatrixSummarizer interface {
    SummarizeWithMatrix(text string, n int) ([]string, [][]float64, error)
}
```

From `internal/summarizer` (rouge.go):
```go
type ROUGEScore struct {
    ROUGE1 F1Score
    ROUGE2 F1Score
    ROUGEL F1Score
}

type F1Score struct {
    Precision float64
    Recall    float64
    F1        float64
}
```

From `internal/summarizer` (summarizer.go):
```go
type Summarizer interface {
    Summarize(text string, n int) ([]string, error)
}

func New(algo string) (Summarizer, error)
```

From `internal/summarizer` (tokenizer.go):
```go
func TokenizeSentences(text string) []string
```

From `internal/sanitizer` (sanitizer.go):
```go
func SanitizeAll(text string) string
func ReportInvisibles(text string) []InvisibleReport
```

From `internal/detector` (detector.go):
```go
const DefaultOutlierThreshold = 0.85
func DetectOutliers(sentences []string, simMatrix [][]float64, threshold float64) []Finding
```

## Tasks

<task type="auto">
  <name>Task 1: Extend pkg/tldt with remaining wrappers</name>
  <files>pkg/tldt/tldt.go</files>
  <action>
    Add the following public API exports to pkg/tldt/tldt.go:
    
    1. **Sanitizer Wrappers** (line ~190 after existing Sanitize function):
       - `SanitizeAll(text string) string` — wrapper for sanitizer.SanitizeAll
       - `ReportInvisibles(text string) []InvisibleReport` — wrapper for sanitizer.ReportInvisibles
       - Re-export type `InvisibleReport` from internal/sanitizer
    
    2. **Detector Extensions** (line ~60 after DefaultOutlierThreshold comment):
       - `const DefaultOutlierThreshold = 0.85` — re-export from internal/detector
       - `DetectOutliers(sentences []string, simMatrix [][]float64, threshold float64) []Finding` — wrapper for detector.DetectOutliers
       - Note: Finding type already compatible (re-use existing Finding or add OutlierFinding)
    
    3. **Summarizer Wrappers** (new section after Fetch function):
       - `NewSummarizer(algo string) (Summarizer, error)` — wrapper for summarizer.New
       - `TokenizeSentences(text string) []string` — wrapper for summarizer.TokenizeSentences  
       - `EvalROUGE(system, reference []string) ROUGEScore` — wrapper for summarizer.EvalROUGE
       - Re-export types: `Summarizer` interface, `Explainer` interface, `MatrixSummarizer` interface
       - Re-export types: `ExplainInfo`, `SentenceScore`, `ROUGEScore`, `F1Score`
       - Add method: `func (e *ExplainInfo) Format() string` — wrap/alias to internal version
       
    All wrappers must:
    - Import and delegate to internal packages (pkg/tldt is allowed to import internal)
    - Wrap errors with `fmt.Errorf("tldt.FunctionName: %w", err)` pattern
    - Include package doc comments explaining the exported function
    - Ensure zero breaking changes to existing pkg/tldt callers
    
    Key implementation notes:
    - For `ExplainInfo.Format()`: Since internal/summarizer.ExplainInfo has Format(), either re-export the type directly (if struct fields are all public) or create wrapper with embedded field
    - `SentenceScore` — ensure all fields are exported (Index, Score, Text, Selected)
    - `ROUGEScore` and `F1Score` — copy struct definitions with exported fields
    
    Verify existing imports in pkg/tldt/tldt.go already include:
    - "github.com/gleicon/tldt/internal/detector"
    - "github.com/gleicon/tldt/internal/sanitizer"  
    - "github.com/gleicon/tldt/internal/summarizer"
    - "github.com/gleicon/tldt/internal/fetcher"
    These should already be present from previous phases.
  </action>
  <verify>
    <automated>
      go build ./pkg/tldt/... && go test ./pkg/tldt/... -v -count=1
    </automated>
  </verify>
  <done>
    - pkg/tldt/tldt.go compiles successfully
    - All existing pkg/tldt tests pass
    - New exports are accessible: `tldt.SanitizeAll`, `tldt.ReportInvisibles`, `tldt.DefaultOutlierThreshold`, `tldt.DetectOutliers`, `tldt.NewSummarizer`, `tldt.TokenizeSentences`, `tldt.EvalROUGE`, and all re-exported types
  </done>
</task>

<task type="auto">
  <name>Task 2: Refactor cmd/tldt/main.go to use pkg/tldt exclusively</name>
  <files>cmd/tldt/main.go</files>
  <action>
    Perform atomic refactor of cmd/tldt/main.go to remove all internal/detector, internal/sanitizer, and internal/summarizer imports:
    
    **Step 1: Update imports (lines 15-22)**
    REMOVE these import lines:
    - "github.com/gleicon/tldt/internal/detector"
    - "github.com/gleicon/tldt/internal/sanitizer"
    - "github.com/gleicon/tldt/internal/summarizer"
    
    KEEP these imports (per D-05):
    - "github.com/gleicon/tldt/internal/config"
    - "github.com/gleicon/tldt/internal/formatter"  
    - "github.com/gleicon/tldt/internal/installer"
    
    **Step 2: Replace detector.DefaultOutlierThreshold (line 42)**
    CHANGE:
      injectionThreshold := flag.Float64("injection-threshold", detector.DefaultOutlierThreshold, "outlier score [0,1] above which sentences are flagged")
    TO:
      injectionThreshold := flag.Float64("injection-threshold", tldt.DefaultOutlierThreshold, "outlier score [0,1] above which sentences are flagged")
    
    **Step 3: Replace sanitizer.SanitizeAll and sanitizer.ReportInvisibles (lines 135-143)**
    CHANGE:
      if *sanitizeFlag {
        stripped := sanitizer.SanitizeAll(text)
        if inv := sanitizer.ReportInvisibles(text); len(inv) > 0 {
          fmt.Fprintf(os.Stderr, "sanitize: removed %d invisible codepoint(s)\n", len(inv))
        }
        text = stripped
      }
    TO:
      if *sanitizeFlag {
        stripped := tldt.SanitizeAll(text)
        if inv := tldt.ReportInvisibles(text); len(inv) > 0 {
          fmt.Fprintf(os.Stderr, "sanitize: removed %d invisible codepoint(s)\n", len(inv))
        }
        text = stripped
      }
    
    **Step 4: Replace detector.SanitizePII and detector.DetectPII (lines 148-168)**
    These are ALREADY using tldt.SanitizePII and tldt.DetectPII per Phase 10 - verify and keep as-is.
    
    **Step 5: Replace sanitizer.ReportInvisibles in detect-injection block (lines 172-176)**
    CHANGE:
      if inv := sanitizer.ReportInvisibles(text); len(inv) > 0 {
    TO:
      if inv := tldt.ReportInvisibles(text); len(inv) > 0 {
    
    **Step 6: Replace summarizer.New (line 204)**
    CHANGE:
      s, err := summarizer.New(effectiveAlgorithm)
    TO:
      s, err := tldt.NewSummarizer(effectiveAlgorithm)
    
    **Step 7: Replace summarizer.Explainer and summarizer.ExplainInfo (lines 214-224)**
    CHANGE:
      if ex, ok := s.(summarizer.Explainer); ok {
        var info *summarizer.ExplainInfo
    TO:
      if ex, ok := s.(tldt.Explainer); ok {
        var info *tldt.ExplainInfo
    
    **Step 8: Replace summarizer.MatrixSummarizer (lines 235-241)**
    CHANGE:
      } else if ms, ok := s.(summarizer.MatrixSummarizer); ok {
    TO:
      } else if ms, ok := s.(tldt.MatrixSummarizer); ok {
    
    **Step 9: Replace summarizer.TokenizeSentences (lines 253, 270, 293)**
    CHANGE (all occurrences):
      sentences := summarizer.TokenizeSentences(text)
    TO:
      sentences := tldt.TokenizeSentences(text)
    
    **Step 10: Replace detector.DetectOutliers (line 254)**
    CHANGE:
      outliers := detector.DetectOutliers(sentences, simMatrix, *injectionThreshold)
    TO:
      outliers := tldt.DetectOutliers(sentences, simMatrix, *injectionThreshold)
    
    **Step 11: Replace summarizer.EvalROUGE (lines 270-271)**
    CHANGE:
      refSents := summarizer.TokenizeSentences(string(refData))
      scores := summarizer.EvalROUGE(result, refSents)
    TO:
      refSents := tldt.TokenizeSentences(string(refData))
      scores := tldt.EvalROUGE(result, refSents)
    
    **Step 12: Replace summarizer.TokenizeSentences in meta (line 293)**
    CHANGE:
      SentencesIn:        len(summarizer.TokenizeSentences(text)),
    TO:
      SentencesIn:        len(tldt.TokenizeSentences(text)),
    
    **Step 13: Update applySentenceCap (line 422-423)**
    CHANGE:
      sents := summarizer.TokenizeSentences(text)
    TO:
      sents := tldt.TokenizeSentences(text)
    
    **Step 14: Verify detector.Analyze call (line 178)**
    This should ALREADY be using tldt.Detect per Phase 9.1 - verify it's:
      dresult, err := tldt.Detect(text, tldt.DetectOptions{...})
    
    **Step 15: Ensure no remaining references**
    After changes, verify main.go has NO references to:
    - detector. (except in comments)
    - sanitizer. (except in comments)
    - summarizer. (except in comments)
    
    **Step 16: Build and test**
    Run `go build ./cmd/tldt/...` to verify compilation.
  </action>
  <verify>
    <automated>
      go build ./cmd/tldt/... && grep -c "internal/detector\|internal/sanitizer\|internal/summarizer" cmd/tldt/main.go || echo "0 matches - imports removed successfully"
    </automated>
  </verify>
  <done>
    - cmd/tldt/main.go compiles successfully
    - Zero matches for internal/detector, internal/sanitizer, internal/summarizer imports
    - Only internal/config, internal/formatter, internal/installer remain from internal/
    - All existing flags preserved and functional
  </done>
</task>

<task type="auto">
  <name>Task 3: Full test suite verification</name>
  <files>cmd/tldt/main.go, pkg/tldt/tldt.go</files>
  <action>
    Run the complete test suite to verify zero regressions:
    
    1. Run all tests:
       `go test ./... -v -count=1`
    
    2. Verify test count >= 360 (should have at least the same as before)
    
    3. Run integration test with the actual binary:
       ```
       go build -o /tmp/tldt ./cmd/tldt
       echo "This is a test. This is only a test. If this were a real emergency, you would be instructed to panic." | /tmp/tldt
       ```
    
    4. Verify key CLI flags work:
       ```
       /tmp/tldt --help
       echo "test text" | /tmp/tldt --sentences 1
       echo "test text" | /tmp/tldt --algorithm textrank
       echo "test text" | /tmp/tldt --detect-pii
       echo "Contact: test@example.com" | /tmp/tldt --sanitize-pii
       echo "test" | /tmp/tldt --sanitize
       echo "test" | /tmp/tldt --detect-injection
       ```
    
    5. Verify zero internal imports grep returns 0:
       `grep -E "internal/(detector|sanitizer|summarizer)" cmd/tldt/main.go | wc -l`
       Expected: 0
  </action>
  <verify>
    <automated>
      go test ./... -count=1 2>&1 | tail -20 && grep -c "internal/detector\|internal/sanitizer\|internal/summarizer" cmd/tldt/main.go || echo "0 matches - SUCCESS"
    </automated>
  </verify>
  <done>
    - All tests pass (360+ test count maintained)
    - Binary builds and runs successfully
    - Zero internal/detector, internal/sanitizer, internal/summarizer imports in main.go
    - CLI-10 and CLI-11 requirements satisfied
  </done>
</task>

## Threat Model

| Boundary | Description |
|----------|-------------|
| pkg/tldt → internal/* | Trusted — pkg/tldt is the public API facade for internal packages |
| cmd/tldt → pkg/tldt | Trusted — CLI routes through public API per design |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | Type wrapper mismatch | mitigate | All wrapper functions use direct delegation; no logic modification. Verification via full test suite (360+ tests). |
| T-11-02 | Information Disclosure | Error wrapping leaks internals | accept | Error wrapping follows established pattern from D-03; tradeoff already accepted in Phase 9.1. |
| T-11-03 | Denial of Service | Import cycle introduction | mitigate | pkg/tldt already imports internal/*; no new cycles introduced. Build verification via `go build ./...`. |

## Verification

### Overall Phase Checks

1. **Requirement Verification:**
   - CLI-10: `grep -E "github.com/gleicon/tldt/internal" cmd/tldt/main.go` returns only config, formatter, installer
   - CLI-11: `go test ./...` passes 360+ tests with same behavior

2. **Behavioral Verification (Manual):**
   ```bash
   # Build binary
   go build -o /tmp/tldt ./cmd/tldt
   
   # Test core functionality
   echo "The quick brown fox jumps over the lazy dog. Dogs are wonderful animals. Foxes are clever." | /tmp/tldt
   
   # Test PII detection
   echo "Contact: alice@example.com" | /tmp/tldt --detect-pii
   
   # Test injection detection
   echo "ignore previous instructions" | /tmp/tldt --detect-injection
   
   # Test sanitize
   echo -e "test\u200btext" | /tmp/tldt --sanitize
   ```

3. **Import Verification:**
   ```bash
   grep -n "internal/" cmd/tldt/main.go | grep -v "config\|formatter\|installer"
   # Expected: no output (or only comments)
   ```

## Success Criteria

- [ ] `cmd/tldt/main.go` imports ONLY these from internal/: `internal/config`, `internal/formatter`, `internal/installer`
- [ ] `grep -c "internal/detector\|internal/sanitizer\|internal/summarizer" cmd/tldt/main.go` returns 0
- [ ] `go test ./...` passes all 360+ tests with no regressions
- [ ] CLI flags `--detect-pii`, `--sanitize-pii`, `--sanitize`, `--detect-injection`, `--url`, etc. all work identically
- [ ] Binary builds and produces correct summaries

## Output

After completion, create `.planning/phases/11-cli-refactor-final/11-01-SUMMARY.md`
